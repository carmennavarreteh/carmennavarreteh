---
version: 3

projects:
  # Disabled directories
  - dir: ./atlantis/test
    name: test-dev
    workspace: dev
    autoplan:
      enabled: true
      when_modified:
        - '**/*.tf*'
  # - dir: ./atlantis/pepe
  #   name: pepe-dev
  #   workspace: dev
  #   autoplan:
  #     enabled: true
  #     when_modified:
  #       - '**/*.tf*'
