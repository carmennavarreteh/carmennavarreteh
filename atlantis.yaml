---
version: 3

projects:
  # Disabled directories
  # First Atlantis in n26-dev-eu
  - dir: ./atlantis/radonium
    name: radonium-dev
    workspace: dev
    autoplan:
      enabled: true
      when_modified:
        - '**/*.tf*'
  - dir: ./atlantis/pepe
    name: pepe-dev
    workspace: dev
    autoplan:
      enabled: true
      when_modified:
        - '**/*.tf*'