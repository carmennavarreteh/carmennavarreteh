---
version: 3

projects:
  # Disabled directories
  - dir: ./atlantis
    workflow: none
    autoplan:
      enabled: false
  # First Atlantis in n26-dev-eu
  - dir: ./atlantis/radonium
    name: radonium-dev
    workspace: dev
    autoplan:
      enabled: true
      when_modified:
        - '**/*.tf*'